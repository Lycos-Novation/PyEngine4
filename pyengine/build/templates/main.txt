from files import *
from files.components import *
from files.scripts import *
from files.utils import *


def launch():
    engine = Engine()
    {SCENES}

    scenes = {SCENES_NAMES}

    game = Game("{NAME}", {WIDTH}, {HEIGHT}, scenes, engine)
    game.run()


if __name__ == "__main__":
    launch()
